# style name or JSON path (default "auto")
style: "C:/Users/cwel/AppData/Local/glow/catppuccin-mocha.json"

# mouse support (TUI-mode only)
mouse: true
# use pager to display markdown
pager: true
# word-wrap at width
width: 80
